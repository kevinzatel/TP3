<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BandProfileActivity"></activity>
        <activity android:name=".MusicianProfileActivity"></activity>
        <activity android:name=".MusicianFirstTimeActivity"></activity>
        <activity android:name=".MusicianEditProfileActivity"></activity>
        <activity android:name=".BandFirstTimeActivity"></activity>
        <activity android:name=".SearchMusicianActivity" />
        <activity android:name=".BandLandingPageActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MusicianLandingActivity" />
        <activity android:name=".MusicianEditProfileActivity" />
        <activity android:name=".MusicianProfileActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>